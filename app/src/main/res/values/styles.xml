<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="textAllCaps">false</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:colorBackground">@color/initial</item>
    </style>

    <!--Splash-->
    <style name="Splash" parent="AppTheme">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowBackground">@drawable/splash_welcome</item>
    </style>

    <!--Skin-->
    <style name="SkinCompatTextAppearance.titleSelectColor" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/skin_title_select</item>
    </style>

    <style name="SkinCompatTextAppearance_titleUnSelectColor" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/skin_title_unselect</item>
    </style>

    <style name="tabLayoutTextSize">
        <item name="android:textSize">15sp</item>
    </style>

</resources>
